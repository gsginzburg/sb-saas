export const environment = {
  production: true,
  dispatchUrl: window.location.protocol + '//' + window.location.hostname + ':8080',
  clusterUrl: window.location.protocol + '//' + window.location.hostname + ':8081'
};
